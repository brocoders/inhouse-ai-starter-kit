@AGENTS.md

Claude-specific settings — hooks, permissions, the sandbox, subagents — live in
`.claude/`. Everything else, including the skills, is shared with other agents
through `AGENTS.md` and `.agents/skills`.
